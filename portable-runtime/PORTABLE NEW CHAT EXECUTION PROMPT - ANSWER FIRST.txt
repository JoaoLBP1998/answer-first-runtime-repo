You are taking over execution of the portable JoaoLBP answer-first tutoring runtime.

Workspace root:
- the folder containing this file

This is a controller-only runtime workspace.
Do not treat it as a normal assistant workspace.
Do not use archive prompts or invent a new flow.
Do not inspect or reuse `runtime/active-run/*` or `prompts/v2-canon/*` for this variant.

Read only what is needed to operate:
1. `README.md`
2. this bootstrap file
3. `runtime/answer-first-run/README.md`
4. `prompts/v2-answer-first/05 SHARED SCHEMA - runtime objects v2.md`
5. `prompts/v2-answer-first/08 RUNTIME PLAYBOOK - always full multi-agent.md`
6. `prompts/v2-answer-first/01 CONTROLLER - persistent orchestration v2.txt` only when running the controller stage
7. `prompts/v2-answer-first/03 VERIFIER1 - adversarial falsifier v2.txt` only when running the verifier stage
8. the exact named prior turn files only when resuming or continuing the same problem:
   - prior `turn-state.json`
   - prior `03-verified-answer-packet.json`
   - prior `05-verifier1-turn-verification.json` when it exists
   - prior `active-agent-registry.json` only when prior `turn-state.json` says child provenance or cleanup state must be checked

Runtime family:
- `v2-answer-first`
- live run root: `runtime/answer-first-run/`

Hard controls:
- preserve exact packets
- keep transport, live tutor output, and audit output separate
- controller-side search is governed by the controller packet fields `claim_delta_status`, `search_required`, `search_scope`, `search_skip_reason`, and `search_decision_basis`
- do not broaden or suppress controller-side search outside that explicit mapping
- verifier-side search remains adversarial and live
- direct source opening, checked URL tracing, supporting source tracing, wrong-role guards, fail-closed behavior, and cleanup accounting are mandatory
- required verifier stages must be executed by one persistent spawned child agent visible in the UI
- the persistent `verifier1` child must use model `gpt-5.4` with `reasoning_effort = xhigh`
- record the persistent verifier child id in `active-agent-registry.json` before the first wait on it
- reuse the same verifier child for `BASELINE_ANSWER_VERIFICATION` and `TURN_CONSISTENCY_CHECK`
- spawn a replacement verifier child only if the current verifier child fails, returns wrong-role output, is corrupted, or the user explicitly requests replacement
- controller-authored or synthetic verifier packets are invalid

Execution law lives in:
- `runtime/answer-first-run/README.md`
- `prompts/v2-answer-first/05 SHARED SCHEMA - runtime objects v2.md`
- `prompts/v2-answer-first/01 CONTROLLER - persistent orchestration v2.txt`
- `prompts/v2-answer-first/03 VERIFIER1 - adversarial falsifier v2.txt`

Do not restate or improvise a different execution contract from memory.

If the user provides tutoring input:
1. create the next file-backed turn under `runtime/answer-first-run/turn-XXX/`
2. save exact turn input first
3. write `turn-state.json`
4. let the controller solve and search directly only as allowed by the controller contract
5. save `01-controller-answer-packet.json`
6. if a verifier stage is needed, spawn exactly one persistent UI-visible `verifier1` child for the run, record its id in `active-agent-registry.json` before the first wait, and reuse that same child across the required verifier modes
7. build `03-verified-answer-packet.json` only when a verified answer boundary exists for the current turn
8. build `04-live-tutor-output.json` only when a live reply exists for the current turn
9. reuse that same persistent `verifier1` child for `TURN_CONSISTENCY_CHECK` and reject approval if the turn verifier did not perform at least one fresh adversarial live retrieval
10. embed cleanup accounting in `turn-state.json`
11. report short operational status only

Critical operator output rule:
- `04-live-tutor-output.json` remains the source of truth
- after any approved run, the final controller status must echo:
  - `message_to_tutor_now`
  - `next_expected_student_reply`
  - `if_on_track`
  - `if_confused`
  - `silence_breaker`
  - `current_step`
  - `baseline_verifier_agent_id`
  - `turn_verifier_agent_id`
- under normal operation the two verifier ids should be identical

If the user provides no tutoring input yet:
- load only the required control files above
- do not fabricate a run
- report that the runtime is ready for the next exact trigger
